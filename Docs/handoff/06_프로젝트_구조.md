# 06. 프로젝트 구조 & 설정 초안

## 디렉토리
```
verihop/
├─ configs/
│  ├─ settings.yaml          # 모델·임계값·상한 (실험 추적: 수정=커밋)
│  └─ relations.yaml         # relation 스키마 (W0.2에서 확정)
├─ src/verihop/
│  ├─ config.py              # yaml → pydantic Settings 싱글턴
│  ├─ models.py              # 명세 §0 데이터 객체
│  ├─ core/                  # 판단 로직 (인프라 직접 호출 금지, infra 경유)
│  │  ├─ correct.py          # [0]
│  │  ├─ decompose.py        # [1]
│  │  ├─ validate_plan.py    # [2] 8종
│  │  ├─ ground.py           # [3]
│  │  ├─ hop.py              # resolve_hop = [4][5][6] (함수 분리: search_candidates/verify_and_pick/reformulate)
│  │  ├─ finalize.py         # [7][8][9]
│  │  └─ executor.py         # ready-set 루프 + 분기 (LangGraph 조립점)
│  ├─ infra/
│  │  ├─ llm.py              # LLM 단일 관문 (재시도·캐싱·비용집계 — 전 콜이 여길 지남)
│  │  ├─ embeddings.py       # 임베딩 + 캐시
│  │  ├─ vector_store.py     # Chroma 어댑터
│  │  └─ graph_store.py      # GraphStore Protocol + NetworkX 구현
│  └─ trace.py
├─ scripts/
│  ├─ w0_yield_check.py      # ★ 첫 태스크 (07 문서)
│  ├─ build_corpus.py        # W1.1
│  ├─ build_index.py         # W1.2
│  ├─ build_graph.py         # W1.3 (+alias W1.4)
│  └─ build_multihop_set.py  # W1.6/W4.2
├─ eval/
│  ├─ run_eval.py            # --mode --set
│  ├─ metrics.py
│  └─ report.py
├─ app/streamlit_app.py
├─ tests/                    # 최소: validate_plan 8종, ground 4경로, verify 규칙, executor 참조해석
└─ data/                     # gitignore (코퍼스·pickle·로그·results)
```

## 원칙
- core는 infra를 통해서만 외부 접근 (테스트에서 mock 주입 가능해야 함)
- LLM 콜은 예외 없이 infra/llm.py 경유 (비용·콜수 지표가 여기서 자동 산출)
- 설정값 하드코딩 금지 — settings.yaml만. Protocol은 GraphStore 하나만 (과설계 금지)
- 재귀 함수로 실행기 구현 금지 (ADR-3)

## configs/settings.yaml 초안
```yaml
llm:
  model: gpt-4o-mini
  temperature: 0.0
  max_retries: 2
embedding:
  model: text-embedding-3-small     # W1.2 스팟체크 미달 시 한국어 특화로 교체
retrieval:
  top_k_paragraphs: 10               # Recall@10 계산 위해 ≥ max(k). LLM 후보추출도 이 상한 사용 (C3)
  use_hyde_definition: true          # sub_query + expected.definition 결합 임베딩
grounding:
  theta_high: 0.90                   # 스냅 확정
  theta_low: 0.75                    # 미만이면 포기+힌트
verification:
  pass_ratio: 0.75                   # 시행 검사 통과 비율. 단 backlink="contradict"는 pass_ratio 무관 hard-veto (03 §0/C1)
  theta_desc: 0.60
  score_lambda: 0.3                  # 벡터 유사도 가중
execution:
  max_requery: 2
  max_depth: 3
  max_hops_total: 5
  tie_epsilon: 0.15                  # margin < ε → tie
  max_active_chains: 3
  memoize: true
paths:
  corpus: data/corpus.jsonl
  chroma_dir: data/chroma
  graph_pickle: data/graph.pkl
  alias: data/alias.json
```

## configs/relations.yaml 초안 (★ W0.2 분포 실측 후 확정 — 아래는 가설)
```yaml
# head_type --relation--> tail_type. direction=fwd가 이 방향.
relations:
  - {name: bornIn,     head: PERSON,   tail: LOCATION, ko: ["태어난", "출생지", "출신"]}
  - {name: diedIn,     head: PERSON,   tail: LOCATION, ko: ["사망한", "죽은 곳"]}
  - {name: locatedIn,  head: LOCATION, tail: LOCATION, ko: ["속한", "위치한", "있는"]}
  - {name: capitalOf,  head: LOCATION, tail: LOCATION, ko: ["수도"]}
  - {name: nationality,head: PERSON,   tail: LOCATION, ko: ["국적", "국가의"]}
  - {name: studiedAt,  head: PERSON,   tail: ORG,      ko: ["공부한", "졸업한", "출신 학교"]}
  - {name: memberOf,   head: PERSON,   tail: ORG,      ko: ["소속", "가입한"]}
  - {name: createdBy,  head: WORK,     tail: PERSON,   ko: ["만든", "작곡한", "쓴", "감독한"]}
  - {name: teacherOf,  head: PERSON,   tail: PERSON,   ko: ["스승", "제자", "가르친"]}
  - {name: occupation, head: PERSON,   tail: TERM,     ko: ["직업", "활동한 분야"]}
fallback: relatedTo        # 스키마 밖 → 검증 약화 모드(backlink 생략)
types: [PERSON, LOCATION, ORG, DATE, NUMBER, WORK, TERM]
operators:
  EARLIER: {in_type: DATE}
  LATER:   {in_type: DATE}
  GREATER: {in_type: NUMBER}
  SMALLER: {in_type: NUMBER}
  SAME:    {in_type: any, out: yesno}
```
